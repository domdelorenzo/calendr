import VueRouter from 'vue-router'

// import components from './components'

const routes = [
  // { path: '/', component: componentname, name: componentname}
]

export default new VueRouter({
  routes,
  mode: 'history'
})